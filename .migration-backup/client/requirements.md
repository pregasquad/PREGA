## Packages
date-fns | Date manipulation for calendar and scheduling
recharts | Charts for the reports dashboard
framer-motion | Smooth animations for UI interactions

## Notes
Authentication uses Replit Auth (OpenID Connect)
Calendar logic requires precise time calculations (15min slots)
